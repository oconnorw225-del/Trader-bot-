# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ inputs.name }}"
name: Repo Cleanup

on:
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Set Git user
      run: |
        git config user.name "Repo Cleanup Bot"
        git config user.email "cleanup@github.com"

    - name: Merge all branches into main
      run: |
        CURRENT=$(git branch --show-current)
        if [ "$CURRENT" != "main" ]; then
          git checkout main
        fi

        for BRANCH in $(git branch -r | grep -v main | sed 's/origin\///'); do
          echo "Merging $BRANCH → main"
          git merge origin/$BRANCH --allow-unrelated-histories -m "Auto-merge $BRANCH into main" || true
        done

        git push origin main

    - name: Delete all other branches (DISABLED)
      run: |
        # DELETION FUNCTIONALITY DISABLED
        # This step has been permanently disabled to prevent accidental data loss
        echo "ERROR: Deletion functionality has been disabled"
        echo "If you need to delete branches, please do so manually via GitHub UI"
        # for BRANCH in $(git branch -r | grep -v main | sed 's/origin\///'); do
        #   echo "Deleting $BRANCH"
        #   git push origin --delete $BRANCH || true
        # done

    - name: Delete stale tags (DISABLED)
      run: |
        # DELETION FUNCTIONALITY DISABLED
        # This step has been permanently disabled to prevent accidental data loss
        echo "ERROR: Tag deletion has been disabled"
        echo "If you need to delete tags, please do so manually via GitHub UI"
        # git tag -l | xargs -n 1 git push --delete origin || true

    - name: Repo cleanup complete
      run: echo "Repository merge complete. Deletion operations have been disabled."
